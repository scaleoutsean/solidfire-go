# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool and VSCode directory
*.out
.VSCode
RELEASE.md
# Dependency directories (remove the comment below to include it)
vendor/
# Example binaries
.create-volumes
.s3-backup
reference/
